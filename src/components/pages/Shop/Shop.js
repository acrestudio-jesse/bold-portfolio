import TitleSection from "../../UI/TitleSection";
import NavBar from "../../UI/NavBar";

const Shop = () => {
  return (
    <section>
      <TitleSection>Shop</TitleSection>
      <NavBar curPage={"Shop"} />
      
    </section>
  );
};

export default Shop;
